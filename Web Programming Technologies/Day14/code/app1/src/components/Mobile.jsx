function Mobile({ model, company, price }) {
  model = 'test'
  return (
    <div>
      <h3>Mobile</h3>
      <div>model = {model}</div>
      <div>company = {company}</div>
      <div>price = {price}</div>
    </div>
  )
}

export default Mobile
